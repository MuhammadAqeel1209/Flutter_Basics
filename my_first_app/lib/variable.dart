void main() {
  // Types of Variables
  // Numbers

  // int age = 25;
  // double height = 5.9;
  // num data = 45;
  // num datas = 4.5;

  // print(age);
  // print(height);
  // print(data);
  // print(datas);

  // Strings
  // String name = 'Aqeel';
  // String greeting = "Hello, Dart!";

  // print(name);
  // print(greeting);

  // Booleans
  // bool isLoggedIn = true;
  // bool hasAccess = false;

  // print(isLoggedIn);
  // print(hasAccess);

  // Lists
  // List<int> numbers = [1, 2, 3, 4];
  // List mixedList = [1, 'Dart', true];
  // var list = [5,6,7,8,9,10];
  // List names = ['Aqeel','Ali','Ahsan'];
  // list.addAll(numbers);
  // names.insertAll(1,list);
  // list.replaceRange(0,3,[10,20,30]);
  // list.removeAt(2);

  // print(numbers);
  // print(mixedList);
  // print(list);
  // print(names);

  // Sets
  // Set<int> uniqueNumbers = {1, 2, 3, 4};
  // print(uniqueNumbers);

  // Maps
  // Map<String, int> studentScores = {
  //   'Aqeel': 90,
  //   'Ali': 85
  // };

  // print(studentScores);
  // print(studentScores.isEmpty);
  // print(studentScores.isNotEmpty);
  // print(studentScores.length);
  // print(studentScores.keys);
  // print(studentScores.values);


  // Runes
  // var heart = '\u2665';  // Unicode for the heart symbol ♥
  // print(heart);

  // Symbols
  // Symbol libraryName = #myLibrary;
  // print(libraryName);

  // Dynamic
  // dynamic values;
  // values = "Hello";  // Can be a String
  // print(values);

  // values = 23;  // Reassign to an integer
  // print(values);

  // Object
  // Object myObject = "Aqeel";  // Could be any type
  // print(myObject);

  // Special Keywords
  // var : Used when you want Dart to infer the type of the variable from the assigned value
  // final : For variables that are only set once and cannot be reassigned
  // const : For compile-time constants. Both `final` and `const` are immutable, but `const` is determined at compile-time, whereas `final` is determined at runtime.

  // var ages = 25;        // Dart infers `int`
  // print(ages);

  // final pi = 3.14;     // Cannot be changed after assignment
  // print(pi);

  // const gravity = 9.8; // Compile-time constant
  // print(gravity);
}
